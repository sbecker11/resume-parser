# scripts: private CLI utilities (e.g. run_merge_on_parsed).
# CLIs: resume-to-flock, render-resume-html, run-merge-on-parsed, validate-parsed-resume. contracts/ holds contract docs, schema, and validator (resume-parser owned).
