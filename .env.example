# LLM API key for job parsing and skill URL enrichment (use at least one)
ANTHROPIC_API_KEY=your-anthropic-key-here
# OPENAI_API_KEY=your-openai-key-here

# If both keys are set (e.g. in shell), force which provider to use:
# LLM_PROVIDER=anthropic
# LLM_PROVIDER=openai
