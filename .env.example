# LLM provider: Anthropic (ANTHROPIC_API_KEY required)
ANTHROPIC_API_KEY=your-anthropic-key-here

# Optional: force LLM_PROVIDER (e.g. if ANTHROPIC_API_KEY is set but other keys exist in env)
# LLM_PROVIDER=anthropic
